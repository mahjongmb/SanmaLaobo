# MBsanma 役割台帳

作成日: 2026-04-05  
更新日: 2026-04-23（運用ルール整理）

## この台帳の目的
- ファイル数増加による認識ズレを防ぐため、役割・責務・依存関係を論理整理する。
- 物理的なフォルダ整理より先に、どのファイルが何を担っているかを固定する。
- 今後はこの台帳を基準に、修正対象ファイルの当たりを付ける。

## 現時点の把握
- 確認済みHTML: 16件（今回このトーク内の実ファイルを再確認）
- 確認済みJS: 44件（今回このトーク内の実ファイルを再確認）
- 未確認JS: 3件（`mbtrial_coupon_client.js` / `mbtrial_coupon_admin.js` / `supabase_client.js`）
- 整理対象JS合計: 47件
- 確認済みCSS: 7件（今回未再確認）
- 確認済みbridge関連: 4件（今回未再確認）
- 備考: 旧台帳でJS扱いしていた `index_app.html / play_app.html 対応初期化処理` は **実ファイルではないためJS件数から除外**。また、旧台帳にあった `visitor_play_ui.js` は今回の提示ファイル群に含まれず、**現行構成に残っているか要再確認** とした。HTML側では、旧台帳にあった `index_public.html` / `play.html` / `verify_app.html` は今回ユーザーが『HTML全部』として提示した現行16件に含まれなかったため、**現行HTML台帳から外し、旧構成メモ扱い** に改める。


## 今回更新メモ（2026-04-23）
- 現在の実装作業は **Codex / Claude Code 主編集** を前提とし、この台帳は **責務整理・修正対象特定・影響範囲確認** の基準として使う。
- 一次正本は **その時点の実ファイル** とし、台帳は **実ファイル確認後に追従更新する整理資料** として扱う。
- 複数ファイルをまたぐ修正が入った場合は、**修正完了後に責務変更だけを台帳へ反映** し、差分未確認のまま台帳を先行更新しない。
- 修正案同士で説明が食い違う場合は、**会話上の説明より最新版ファイル本文を優先** する。

## 今回更新メモ（2026-04-08）
- `index_app.html` を **SANMA LAB のトップハブ** として再整理。ログイン状態表示、対局モード / 検証モード、下段5ボタン（牌譜再生 / 分析 / 成績管理 / ルール変更 / 設定変更）を持つ入口として扱う。
- `play_app.html` / `app_play_ui.js` 側で、**対局モード / 検証モードの開始情報をログへ渡す補強** を実施。牌譜一覧で検証モード半荘が未分類に落ちにくい前提を追加した。
- `replay_app.html` は **半荘一覧ページ** へ更新。保存済み半荘ログのカード一覧を出し、選択した半荘を `replay_view_app.html` へ送る入口として扱う。
- `replay_view_app.html` を新規追加。選択した1半荘を東1局1手目から追う **再生専用ページ** として扱う。
- `replay_app_ui.js` / `replay_view_app_ui.js` を新規追加。一覧描画と再生ビュー描画を分離した。
- `analysis_app.html` は **仮ページから独立した分析ダッシュボード入口** へ更新。トップの分析ボタン先で、保存済み半荘ログから局単位スタッツを見るページとして扱う。
- `records_app.html` は引き続き **自分の成績管理ページの仮置き**。方針として、今後は「成績管理=対局モードの自分成績」「分析=検証モードの全体傾向」で役割を分ける。
- `analysis_ui.js` は、現状は **牌譜再生・分析オーバーレイ側** に「分析」タブを後付けするJSとして扱う。トップの `analysis_app.html` とは役割を分離して保持する。
- `match_log.js` / `log_normalizer.js` / `log_metrics.js` は、アプリ版の **生ログ → 正規化 → 集計** の3層として、牌譜再生・分析・CPU比較の土台であることを継続して明記する。
- `log_metrics.js` は、最低限件数集計だけでなく **分析ページ向けサマリー集計** を返す役へ拡張。現在は局単位の指標整理が中心で、将来の役/打点詳細拡張の受け皿でもある。

## 今回更新メモ（2026-04-09）
- `match_log.js` を **IndexedDB優先の半荘ログ保存基盤** として整理。アカウント/ローカル別スコープ保存、旧localStorageログ移行、`getStoredLogsAsync()` / `refreshStoredLogsAsync()` を持つ読込基盤として扱う。
- ログ保存内容は **商品版寄りの最適化** を反映。局開始スナップショットでは `wall` / `deadWall` を保持しつつ、`cpu_open_snapshot` / `cpu_discard_snapshot` / `cpu_api_bridge_request` / `cpu_api_bridge_response` など重いbridge系イベントは保存対象から外す前提を明記する。
- `match_log.js` の保存時整形では、CPU seat meta は `openProfileKey` / `discardStyleKey` 中心に縮約し、external / shadow 比較の残骸を保存側で薄くする方針へ整理した。
- `analysis_app.html` は **async読込前提の独立分析ダッシュボード** として整理。保存済み半荘ログを `getStoredLogsAsync()` で読み、読込中表示と全席均等サンプル前提の局単位サマリーを出すページとして扱う。
- `main.js` はアプリ版半荘ログの **開始メタ生成 / 局開始スナップショット投入 / 半荘終了保存** の接続点を持つ司令塔として扱う。`startNewHanchan()` / `startNewKyoku()` / `movePostAgariFlowFromResultToNext()` から `MBSanmaMatchLog` を呼ぶ前提を追記対象とする。
- `app_play_ui.js` は、アプリ版プレイ画面の後付けUIだけでなく **モード表示 / 牌譜再生・分析オーバーレイ / 半荘終了履歴保存 / クラウド同期補助** を持つファイルとして整理し直す。
- `records_app.html` を **成績管理ページ本体** として整理。概要 / グラフ / 詳細 の3タブで、対局モードの自分半荘成績を見る独立ページへ更新した。
- `records_app.html` は、対象半荘数・保存先フィルタ、最新半荘カード、最近の半荘一覧、サマリーカード、推移グラフ、詳細スタッツ群を持つ前提へ変更した。
- `records_app.html` の役割は **分析ページとは完全分離** と明記。分析ページは検証モードの全体傾向、成績管理ページは対局モードの自分成績を扱う。
- `log_metrics.js` は、分析向け集計に加えて **成績管理ページ向け `buildRecordsSummary()`** を持つ集計中核へ拡張。半荘単位の順位 / スコア / チップ、局単位の攻撃・守備・条件別指標を返す。
- `log_metrics.js` の成績管理向け集計では **seat0（あなた）固定** を前提とし、フィルタ正規化も `matchMode: "normal"` 固定に寄せた。
- `log_metrics.js` のチップ集計は、点数 `delta` を誤って拾わず **`chipInfo` / `resultMeta.chipInfo` / chip系サマリーのみ参照** する方針へ整理した。

## 今回更新メモ（2026-04-10）
- `yaku.js` の **流し倍満成立条件** を整理。**么九牌/字牌のみの河、本人が鳴いていない、本人の河が鳴かれていない、リーチしていない、暗槓していない、ただし北抜きは可** という前提を台帳へ明記する。
- `actions.js` は山枯れ流局時に **流し倍満成立者を通常アガリ扱いで検出し、複数人同時成立なら結果キューを組んで順送り表示へ渡す入口** を持つファイルとして整理し直す。
- `result.js` は通常のツモ/ロン/流局だけでなく、**流し倍満を結果確認画面で表示する専用分岐** を持つ前提へ更新。役名表示、倍満表示、複数成立時の順送り表示に対応する。
- `main.js` の終局後フローは、ダブロン系だけでなく **流し倍満の複数人同時成立でも結果確認画面を順送りし、最後にまとめて精算へ進める** 前提へ更新した。
- `main.js` のデバッグ局開始系は、**通常半荘を一度起動してから上書きする方式ではなく、持ち越し状態・CPU残留状態を直接初期化してからシナリオ局を安全に構築する方式** に整理した。
- `debug.js` は、デバッグシナリオ入力UIとして継続しつつ、**安全巡目上限の採用・開始時の多重押下回避・無理な指定を抑える前提** を追記対象とする。
- `index_app.html` / `app_play_ui.js` / `main.js` のアプリ版起動フローは、**検証モードの初期値は自動、ただし半荘中の設定変更で手動へ切替可能、閉じて入り直したときは再び検証モード初期値の自動へ戻る** 仕様で整理する。
- `app_play_ui.js` のモードバッジは **起動モード固定文言ではなく、現在の自分操作モードへ追従する表示** を持つ前提へ更新した。
- 運用ルールとして、**原因特定や修正時は既見ファイルでも古い可能性を前提に、常に最新版を再提示してもらってから判断する** 方針を台帳へ追記する。

## 今回更新メモ（2026-04-15）
- `rules_app.html` を **アプリ版ルール変更ページの見た目土台** として更新。トップへ戻るだけの仮ページ段階を抜け、上部ヒーロー、1段タブ、各設定カードを持つ構成として整理する。
- `rules_app.html` は **概要 / 点数・精算 / 使用牌 / 基本 / 補足 / 採用役** の6タブを持ち、各タブ内の項目を縦並びで切り替える UI 入口として扱う。
- 入力UIは、**採用/不採用スイッチ、3択以上の選択ボタン、数値入力欄、仮プレースホルダー** を使い分ける方式で整理した。
- `rules_app.html` の採用役タブは、**基本役を翻数帯ごとにチップ化して個別切替** し、別枠で **三麻ローカル役** と **萬子のホンイツ 3択** を持つ構成として整理する。
- `rules_app.html` は**この時点では**、**sessionStorage でタブ状態と入力値を保持する UI 単体ページ** として整理していた。後続更新で保存接続・一部ルール反映が進んだため、この記述は 2026-04-15 時点の履歴メモとして残す。


## 今回更新メモ（2026-04-16）
- `play_app.html` は盤面HTML束ねだけでなく、`window.MBSanmaRulesConfig` を先行生成する **ルール設定ブートストラップ** を内包する。`mbsanma_rules_settings_v1` を正本に、旧 `mbsanma_rules_ui_v2` を後方互換で読む前提へ更新する。
- `rules_app.html` は **sessionStorage / localStorage / `mbsanma_rules_settings_v1` への保存接続** を持つルール変更ページとして整理し直す。現時点で **半荘戦 / 東風戦、持ち点 / 返し点、連荘条件、ウマ / クビ、トビ終了、箱下精算、本場、聴牌料、コールド終了** はアプリ版対局へ反映される前提を追記する。
- `main.js` は `MBSanmaRulesConfig` 経由で **半荘戦 / 東風戦** と **連荘条件** を読んで局進行へ反映し、あわせて **検証高速モードの速度プロファイル管理** と設定オーバーレイ描画も担う司令塔として整理する。
- `tensukeisan.js` は `MBSanmaRulesConfig` 経由で **ツモ損 / 100点単位あり / 切り上げ満貫 / 流し倍満種別 / 本場点** を読む点数計算層として整理する。
- `seisan.js` は `MBSanmaRulesConfig` 経由で **持ち点 / 返し点 / 連荘条件 / 箱下 / トビ終了 / コールド終了 / 本場 / 聴牌料** を読む精算層として整理する。
- `yaku.js` は通常役側に **一盃口 / 二盃口 / 三色同刻 / 三槓子 / 一気通貫 / 混全帯么九 / 純全帯么九** を含み、あわせて **流し倍満の満貫/なし切替** をルール設定から読む前提へ更新する。
- `analysis_app.html` は独立分析ページとして **全体 / 和了 / リーチ / 放銃 / 被ツモ / 流局 / 役** を見る6タブ構成へ進み、UI上は保存済み検証対局（`matchMode: "batch"`）を主対象にする前提で整理する。
- `log_metrics.js` は独立分析ページ向けに、**全体 / リーチ / 副露 / 和了 / 放銃 / 被ツモ / 流局 / 役** まで含むサマリー返却層として整理し直す。
- `turn.js` は通常進行だけでなく、**待ち形分類（両面 / 三面張 / 多面張 / 愚形系）を含むリーチ詳細ログ補助** を持つ前提へ更新する。

## 今回更新メモ（2026-04-18）
- JS台帳を **実在するJSファイル単位** で再整理し、件数を 49件 → **47件** に補正した。今回未提示の `mbtrial_coupon_client.js` / `mbtrial_coupon_admin.js` / `supabase_client.js` は **未確認** と明記した。
- 旧台帳でJS欄に入っていた **`index_app.html / play_app.html 対応初期化処理`** は、説明上は重要だがファイルではないため **HTML側の責務に寄せる考え方** に修正した。
- `core.js` は、従来の牌定義・共通土台に加えて **色付き牌構成（r / b / g / n）と牌構成正規化の正本** として扱うよう更新した。
- `yaku.js` は、固定の赤5/虹北前提ではなく **色付き牌の祝儀対象設定・詳細ドラ枚数/祝儀枚数・後付け制御** を読む役判定/ボーナス集約層として整理し直した。
- `main.js` は、従来の局進行司令塔に加えて **検証高速モードの速度プロファイル、終局後自動進行段階管理、半荘ログ完了フック** を持つ前提へ更新した。
- `match_log.js` は **IndexedDB優先 + account/localスコープ + 旧保存形式移行** を持つ保存基盤として、現行の役割を再確認した。
- `debug.js` は、従来の局面再現UIに加えて **色付き牌構成を読んだ候補生成・枚数制限反映** を行う前提へ更新した。
- `mbtrial_ui.js` は、MB体験版UIに加えて **ローカルのクーポン記録保持、使用済み状態同期、API有効時のクーポン取得/同期** を含む前提へ更新した。

- HTML台帳は、今回ユーザーが提示した **現行16ファイル** を基準に再確認した。これにより、旧台帳に残っていた `index_public.html` / `play.html` / `verify_app.html` は **現行正本から外す** 形に整理した。
- `index.html` は現行で **`index_mbtrial.html` への即時リダイレクト専用入口**。サイトの正面導線はMB体験版トップを向く前提で固定した。
- `index_mbtrial.html` は、**チャレンジ開始 / チャレンジ案内 / 取得クーポン / 料金・営業時間 / アクセス / LINE / 麻雀王国** まで持つ、店外向けトップ導線として再整理した。
- `play_mbtrial.html` は、**trialセッション確認付きの体験版プレイ本体**。共通ゲームJS束ねに加えて、`mbtrial_coupon_client.js` / `mbtrial_ui.js` を読む前提を明記する。
- `coupon_admin.html` は、**検索 / 詳細表示 / 一覧更新 / 未使用絞り込み / 使用済み更新** を持つ店側クーポン管理画面として整理し直した。加えて、**Supabase設定をHTML内で差し込んでいる点** と **本格運用には認証補強が必要** という注意文も役割に反映する。
- `index_app.html` は、SANMA LABトップとしての役割に加えて、**Supabase設定差し込み / ログインダイアログ / アカウント発行 / ログアウト / 起動モード保存** を持つ入口HTMLとして再整理した。
- `index_app_debug_login.html` は、`index_app.html` のデバッグ派生で、**ログイン調査ログ表示・状態行・デバッグ消去UI** を持つ検証用トップとして独立明記する。
- `play_app.html` は、**account/local セッション確認ガード** を先頭で持ち、その上で **`window.MBSanmaRulesConfig` ブートストラップ、共通ゲーム本体、ログ保存、分析UI、Supabaseクライアント読込** を束ねるアプリ版結節点として更新する。
- `index_internal.html` は、**セッションガードなしで直接起動できる内部盤面HTML** として再整理した。`debug.js` まで含む共通ゲーム束ねを読み、ローカル検証・直接起動用の入口として扱う。
- `rules.html` は、**店の公開ルール案内ページ** として独立明記する。ヒーロー、概要カード、stickyナビ、FAQ/CTA を持つ読み物ページであり、アプリ版の可変設定UIとは別物とする。
- `price.html` は、**料金・営業時間の公開案内ページ** として独立明記する。三麻 / ライト三麻 / 四麻 / 貸卓料金、営業時間、予約導線を掲載する固定情報ページである。
- `rules_app.html` は、保存入口としての従来整理に加えて、**牌構成オーバーレイ編集、色付き牌サマリー、祝儀対象牌の詳細編集（面前/鳴き別ドラ枚数・祝儀枚数）** まで含む現行UIとして反映した。
- `settings_app.html` は、もはや仮ページではなく、**対局モード / 検証モード / 環境設定 の3タブ構成・保存付き設定UI** として整理し直した。さらに、**検証モードタブの4項目は実際の対局開始設定へ反映される** 前提を追記する。

## HTMLファイル台帳
| ファイル名 | 区分 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|---|
| index.html | 公開入口 | ルート入口。現行では `index_mbtrial.html` へ即時リダイレクトするだけの正面導線。 | 無 | 正面はMB体験版トップを向く。実質的には公開用リダイレクトHTML |
| index_mbtrial.html | 店外体験版トップ | MB体験版トップ。1半荘チャレンジ開始、チャレンジ案内、取得クーポン、料金・アクセス・外部リンク導線を持つ。 | 無（セッション開始処理のみ） | ブラウザ保存注意文あり。`mbtrial_coupon_client.js` を読む前提で **Supabase設定をHTML内で差し込む** |
| play_mbtrial.html | 店外体験版プレイ本体HTML | MB体験版の盤面DOM、オーバーレイ、結果画面、上部3ボタン、共通ゲームJS/CSS束ね。 | 無（HTML殻） | **trialセッション確認あり**。`mbtrial_coupon_client.js` と `mbtrial_ui.js` を読込 |
| coupon_admin.html | 店側クーポン管理HTML | 店側用のクーポン確認ページ。コード検索、詳細表示、一覧表示、未使用絞り込み、使用済み更新導線を持つ。 | 無（HTML殻） | `mbtrial_coupon_client.js` / `mbtrial_coupon_admin.js` を読込。**Supabase設定差し込みあり**。画面内に「認証補強が必要」の注意文も持つ |
| rules.html | 公開ルール案内ページ | 麻雀MBの三麻ルールを一般向けに案内する固定ページ。概要、ルール各章、FAQ、CTA を1ページで読むための公開導線。 | 無 | **アプリ版の可変ルールUIではない**。stickyナビと読み物レイアウトを持つ |
| price.html | 公開料金・営業時間ページ | 麻雀MBの料金・営業時間・予約方法を案内する固定ページ。 | 無 | 三麻 / ライト三麻 / 四麻 / 貸卓料金、営業時間、予約導線を掲載する公開情報ページ |
| index_app.html | アプリ版入口 | SANMA LAB のトップハブ。ログイン状態表示、対局モード / 検証モード、牌譜再生 / 分析 / 成績管理 / ルール変更 / 設定変更の入口を持つ。 | 無（セッション開始処理のみ） | **Supabase設定差し込み / ログインダイアログ / アカウント発行 / ログアウト / 起動モード保存** を持つアプリ版専用トップ |
| index_app_debug_login.html | アプリ版ログイン調査用トップ | `index_app.html` のデバッグ派生。ログイン調査ログ、状態行、デバッグ消去UIを持ち、ログイン不具合切り分けに使う。 | 無（セッション開始処理のみ） | 見た目と基本導線は `index_app.html` 系だが、**debugLog / debugStateLine** を持つ検証用入口 |
| play_app.html | アプリ版プレイ本体HTML | アプリ版の盤面DOM、オーバーレイ、結果画面、設定画面、6ボタン上部UI、ログ/分析基盤の読込を束ねる本体HTML。 | 無（HTML殻 / ルール設定読込あり） | **account/local セッション確認ガード** を先頭で持つ。`window.MBSanmaRulesConfig` を先行生成し、`supabase_client.js` / `match_log.js` / `log_normalizer.js` / `log_metrics.js` / `app_play_ui.js` / `analysis_ui.js` を読込 |
| replay_app.html | アプリ版牌譜一覧ページ | 保存済み半荘ログをカード一覧で表示し、選択した半荘を再生ページへ渡す。 | 無（HTML殻） | `match_log.js` / `log_normalizer.js` / `replay_app_ui.js` 読込あり |
| replay_view_app.html | アプリ版牌譜再生ビュー | 選択した1半荘を局→手順で追う再生専用ページ。 | 無（HTML殻） | 局選択 / 再生速度 / step slider / イベントログ表示を持つ。`match_log.js` / `log_normalizer.js` / `replay_view_app_ui.js` 読込あり |
| analysis_app.html | アプリ版分析ページ | 保存済み検証対局を読み、全体 / 和了 / リーチ / 放銃 / 被ツモ / 流局 / 役 の傾向を見る独立分析ページ。 | 無（HTML殻） | `match_log.js` / `log_normalizer.js` / `log_metrics.js` を読込。UI上は `matchMode: "batch"` を主対象にした検証結果まとめページ |
| records_app.html | アプリ版成績管理ページ | 対局モードの自分成績を見る本体ページ。概要 / グラフ / 詳細 の3タブ、サマリーカード、最近の半荘一覧、詳細スタッツ表示を持つ。 | 無（HTML殻） | `match_log.js` / `log_metrics.js` を読込。分析ページとは役割を分離し、対象は seat0（あなた）固定 |
| rules_app.html | アプリ版ルール変更ページ | ルールカスタムUI兼保存入口。概要 / 点数・精算 / 使用牌 / 基本 / 補足 / 採用役 の6タブで入力UIを切り替える。 | 無（UI保存のみ） | `sessionStorage` / `localStorage` / `mbsanma_rules_settings_v1` へ保存する。**牌構成編集オーバーレイ、色付き牌サマリー、祝儀対象牌の詳細編集** まで含む現行正本UI |
| settings_app.html | アプリ版設定変更ページ | 対局モード / 検証モード / 環境設定 の3タブで、設定値を保持する独立設定UI。 | 無（UI保存のみ） | `sessionStorage` / `localStorage` / `mbsanma_settings_values_v1` へ保存。**検証モードタブの4項目は検証モード開始時の実設定へ反映** |
| index_internal.html | 内部プレイ本体HTML | セッションガードなしで直接起動できる内部盤面HTML。 | 無（HTML殻） | 共通ゲーム本体を束ね、`debug.js` まで読込。ローカル検証・直接起動用の内部入口 |

## CSSファイル台帳
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| base.css | 共通変数、全体レイアウト、盤面ステージ、上部3ボタン、アカウントバッジの土台。 | 無 | `:root` 変数と `.gameBoard` 基本寸法の中心。画面全体の基準CSS |
| center.css | 中央UIの見た目。局表示、供託、ドラ、山/王牌、3方向の点数/チップ表示。 | 無 | `render_center.js` が生成するDOMの表示先 |
| cpu.css | 左右CPUエリアの見た目。CPU手牌、CPUツモ牌、CPU河、CPU北、副露の配置。 | 無 | 左右CPUの位置・向き・サイズ調整の中心 |
| hand.css | 自分の手牌ゾーン、アクションボタン、受け入れ表示 `#stats`、選択牌/新牌表示。 | 無 | 手牌操作UIと行動ボタンの主担当CSS |
| overlay.css | ツモ/ロン/流局/結果確認オーバーレイ、カン/ポン/リーチ演出、drawOverlay、結果画面レスポンシブ。 | 無 | 各種演出と結果画面の見た目全般 |
| rightArea.css | 自分側の右下エリア表示。北表示 `#peis` と副露表示 `#melds` の見た目。 | 無 | 自分側の北/副露表示専用 |
| river.css | 自分の河の見た目。`#river` の牌サイズ、位置、リーチ宣言牌の回転。 | 無 | 自分河と点数位置微調整 |

## JSファイル台帳

### コア/共有状態
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| core.js | 牌定義・共通ユーティリティ・DOM参照・グローバル基盤。山生成、画像生成、共通参照APIに加え、**色付き牌構成（`r / b / g / n`）の正規化と生成元** を持つ。 | 基盤 | `TILE_TYPES` / `TYPE_TO_IDX` / `TILE_COLOR_KEYS`、`buildDefaultTileComposition()`、`normalizeTileComposition()` を持つ現行の正本 |
| core2.js | CPU追加状態の保管庫。CPUリーチ/宣言牌表示ID/ツモ牌/副露/北/デバッグ用指定順打牌フラグをまとめる。 | 有 | CPU系状態参照APIが集約。`isCpuHandOpen` や CPU河・副露・北の参照口もここ |

### 局進行/プレイヤー行動
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| main.js | 起動・イベント紐付け・局開始/次局進行・終局後フロー・設定UI反映・自動対局バッチ進行の司令塔。 | 有 | `startNewHanchan()` / `startNewKyoku()` / `startNextKyoku()` 系に加え、**検証高速モードの速度プロファイル管理、終局後自動進行段階管理、`MBSanmaMatchLog.finishMatch()` 呼び出し** もここ |
| turn.js | 通常ターン進行の司令塔。誰の番か、捨て牌フェーズか、各種タイマー制御、プレイヤーAI補助を担当。 | 有 | 局中の主進行。**速度プロファイル参照 (`getGameSpeedMs`) と待ち形分類を含むリーチ詳細ログ補助** も持つ |
| turn2.js | `turn.js` へ後乗せする進行パッチ。CPUロン・リーチ成立/不成立後決め・アガリ結果キュー対応。 | 有（パッチ） | リーチ宣言牌に副露が入った時の成立扱い、CPUロン優先処理、ダブロン用キューAPIを差し込む |
| actions.js | 共通アクション処理。ツモ/ロン/流局/流し用オーバーレイ、アガリ演出位置、卓→結果→次局の橋渡しを担当。 | 有 | **追加ロン/流しオーバーレイの複製表示**、流し結果キューとの接続、`MBSanmaMatchLog` へのアガリイベント投入もここ |
| call.js | 自分の鳴き判定/UI/実行（ロン・ポン・明槓・スキップ）とプレイヤー副露AI補助。 | 有 | プレイヤー鳴きの状態変更担当。**open/special AI 用の分岐**、visible counts 構築、`call_pass` を含むログ材料もここ |
| kan.js | 自分手番中の暗槓・加槓実行。明槓は `call.js` 側。 | 有 | カン演出も持つ。表示用牌情報を保ったまま副露へ組み替える |
| pei.js | 自分の北抜き実行。`drawn` / `hand13` / `peis` / 王牌補充を扱う。 | 有 | リーチ中例外も含む。`MBSanmaMatchLog` への `pei` / `draw` 記録あり |
| riichi.js | リーチ選択→成立→自動ツモ切り停止、リーチ後の特殊行動停止判定を担当。 | 有 | **速度プロファイル連動の演出/自動ツモ切り遅延**、自動再試行制御あり |
| furiten.js | 既存ロジックへ後付けでフリテン管理を差し込む。河フリテン・見逃しフリテン・表示。 | 有（ラップ） | 他ファイル無改造で導入。**リーチ後見逃しの永久フリテン** にも対応 |

### 描画/UI
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| render.js | 描画/UIの司令塔。ボタン表示条件、リーチ/ツモ/カン可否判定、各描画API呼び分け。 | 無 | render系の入口。**描画専用で状態変更しない** 前提を明記 |
| render_center.js | 中央UI描画。局表示、供託、ドラ、山/王牌、中央点数DOM生成と更新。 | 無 | DOM不足時の生成も担当。供託棒画像フォールバックあり |
| render_cpu.js | 左右CPUの手牌/河/公開表示の描画。終局時や流局テンパイ時の手牌公開も扱う。 | 無 | `isCpuHandOpen`、勝者席、流局テンパイ席に応じて開閉を切り替える |
| render_right.js | 右エリアの北/副露描画。副露表示の詰めや向きも制御。 | 無 | 自分側の北/副露表示。**calledIndex と from を使った副露向き制御** を持つ |
| render_stats.js | 受け入れ/待ち/シャンテン表示の描画。 | 無 | **通常時とリーチ選択時の両方** に対応し、待ち/受け入れをタイル画像付きで出す |
| result.js | 結果確認画面(`resultOverlay`)の表示専用。open/close API、レスポンシブ再配置、ズーム防止ガードを持つ。 | 無 | 通常結果に加え、**複数結果の順送り表示・上段ドラ帯再配置** もここ |

### 手牌解析/役/点数/精算
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| shanten.js | 一般手・七対子・国士を含むシャンテン/受け入れ計算の基盤。 | 無 | 牌種並び変更に強い作り。`TYPE_TO_IDX` 依存を動的に吸収 |
| yaku.js | 通常役判定とドラ/赤/北/祝儀などボーナス素材の集約。 | 無 | 純関数中心。**色付き牌ごとの祝儀対象設定、詳細ドラ枚数/祝儀枚数、後付け制御、流し満貫/倍満判定** を含む現行の中核 |
| yakuman.js | 役満判定を通常役から分離。`yaku.js` 側の共通土台を利用。 | 無 | `yaku.js` から利用前提 |
| fu.js | 符計算専用。七対子固定25符、平和ツモ20符などを扱う。 | 無 | 点数計算から独立 |
| tensukeisan.js | 三麻点数表のlookup。結果画面や将来の持ち点増減用の点数返却。 | 無 | **通常表/100点単位あり表/ツモ損あり表/切り上げ満貫** を切り替える計算層 |
| seisan.js | 点数移動・供託・流局テンパイ料・飛び・オーラス終局判定。 | 有 | `applyPendingRoundSettlement()` に実際の点数変更を集約。**ルール設定から持ち点/返し点/本場/聴牌料/連荘条件等を取得** |

### CPU本番行動
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| cpai.js | CPU打牌判断の本体。旧来ロジックの候補生成/比較や改善牌枚数計算。 | 無（判断） | 選択器本体。legacy候補列挙と比較ロジックを保持 |
| cpu_actions.js | CPU特殊行動。北抜き連続処理、暗槓候補収集など。 | 有 | CPU行動実行側。**`MBSanmaMatchLog` への pei/draw 記録** も持つ |
| cpu_calls.js | CPU副露実行。ポン/明槓の候補席・副露生成・副露後打牌選択補助。 | 有 | CPU鳴き実行側。表示牌情報を保ったまま meld 生成 |
| cpu_call_policy.js | CPU副露判断の静的ポリシー値。許可条件/待ち条件など。 | 無 | pon/minkan 別の基本条件と席別上書き定義 |

### CPU評価/プロファイル/ログ
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| cpu_open_profiles.js | CPU副露判断プロファイル定義。守備/速度/打点/前のめり等の重み集合。 | 無 | 席ごとの副露エンジン種別も保持 |
| cpu_open_eval.js | CPU副露評価器。snapshotを採点し、内部AIなら何を選ぶか返す影武者。 | 無 | render/state非依存 |
| cpu_snapshot.js | CPU副露候補時点の局面snapshot土台。履歴や意思決定ログの共通基盤。 | 無 | **色付き牌の `imgCode` / `colorKey` 正規化** を含む |
| cpu_discard_profiles.js | CPU打牌評価プロファイル定義。internal/external/legacy切替も保持。 | 無 | 重みセットライブラリ |
| cpu_discard_eval.js | CPU打牌評価器。snapshot候補を採点し、内部AIの選択を返す。 | 無 | dora保持・危険度・速度などを重みで採点 |
| cpu_discard_snapshot.js | CPU打牌snapshot/decision log。比較検証用の候補記録土台。 | 無 | **`r4z -> n4z` 正規化と色キー保持** を含む |
| cpu_api_bridge.js | ブラウザ→ローカルAPI橋渡し。外部AI呼び出しhookとフォールバック窓口。 | 無 | **副露用/打牌用の別endpoint** と sync decision hook を持つ |

### 公開版/保存/デバッグ/パッチ
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| supabase_client.js | Supabase RPC呼び出しまとめ。アカウント発行/読込/保存。 | 無 | **今回未提示のため役割説明は旧台帳ベース。要再確認** |
| debug.js | シナリオデバッグオーバーレイ。局設定・手牌・ドラ・王牌・山先頭・巡目指定UI。 | 無（UI側） | 実際の状態変更本体は `main.js`。**色付き牌構成を読んだ候補生成、枚数制限反映、安全開始リトライ** を持つ |
| doubleRonOverlayPatch.js | ダブロン演出/結果順番表示パッチ。ロン演出と結果画面を順番表示。 | 補助 | `actions/main/turn2/result` を壊さず差し込む |

### アプリ版土台/保存/ログ/再生/分析
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| app_play_ui.js | アプリ版プレイ画面UI。上部ボタン差し替え、ルール/成績/設定/牌譜再生オーバーレイ、ローカル/アカウント保存を担当。 | 局状態は触らない / 保存状態は触る | **トップボタン再配線、履歴保存、tracker管理、`MBSanmaSupabase` への snapshot 同期、半荘終了フック** を持つ |
| match_log.js | 半荘/局ログ基盤。局開始スナップショット、イベント、開始メタ、精算結果を **IndexedDB優先** で保存し、async読込と旧localStorage移行も担う。 | 保存状態は触る | **account/local スコープ分離、旧キー移行、保存件数上限管理** を持つ現行正本 |
| log_normalizer.js | 生ログを1アクション単位へ正規化する層。CPU判断束ね・表示/解析向けの中間データを作る。 | 無 | ログ表示/リプレイ/分析の共通中間層 |
| log_metrics.js | 正規化ログや保存ログから件数・基礎指標を作る集計層。分析向け `buildAnalysisSummary()` と成績管理向け `buildRecordsSummary()` を持つ。 | 無 | **全体 / リーチ / 副露 / 和了 / 放銃 / 被ツモ / 流局 / 役** を含む集計を返す |
| analysis_ui.js | アプリ版 条件指定分析UI。牌譜再生・分析オーバーレイへ「分析」タブを後付けし、保存済みログから条件指定集計を表示する。 | 無 | `limit / matchMode / dealer / junmeBand / waitShape` を持つ条件指定分析 |
| replay_app_ui.js | アプリ版牌譜一覧UI。保存済み半荘ログの一覧描画、モードフィルタ、再生ビュー遷移を担当。 | 無 | `replay_app.html` 専用。session/account バッジ表示あり |
| replay_view_app_ui.js | アプリ版牌譜再生ビューUI。局選択、手順送り、オート再生、イベントログ表示を担当。 | 無 | `replay_view_app.html` 専用。**step slider と自動再生状態管理** を持つ |

### MB体験版/クーポン管理
| ファイル名 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|
| mbtrial_ui.js | MB体験版専用UI。上部ボタン差し替え、案内/ルール/取得クーポンオーバーレイ、終局後クーポン表示を担当。 | 局状態は触らない / 保存状態は触る | **deviceId生成、localStorage上のクーポン記録保持、使用済みマーカー同期、API有効時のクーポン取得/照会** を含む |
| mbtrial_coupon_client.js | MB体験版クーポン用 Supabase RPC クライアント。発行、device取得、code取得、使用済み更新、一覧取得をまとめる。 | 無 | **今回未提示のため役割説明は旧台帳ベース。要再確認** |
| mbtrial_coupon_admin.js | 店側クーポン管理画面ロジック。コード検索、詳細表示、一覧描画、未使用絞り込み、使用済み更新を担当。 | 局状態は触らない / クーポン状態は更新 | **今回未提示のため役割説明は旧台帳ベース。要再確認** |

## bridge / ローカルAPI連携ファイル台帳
| ファイル名 | 区分 | 主な役割 | 状態変更 | 備考 |
|---|---|---|---|---|
| README.txt | 説明書 | bridge同梱ファイル一覧と目的の要約。 | 無 | CPU打牌external連携基盤パッチの概要メモ |
| server.js | Node.js ローカル橋渡しサーバー | ブラウザから送られたCPU副露/打牌snapshotを受け、OpenAI Responses APIへ問い合わせ、判定結果を返す。 | 有（サーバー側状態/履歴） | `/cpu/open-call-decision` と `/cpu/discard-decision`、`/health`、`/debug/*` を提供 |
| start_bridge_server.bat | 起動補助 | カレントディレクトリを移して `node server.js` を起動する。 | 無 | ローカルbridgeの簡易起動用 |
| start_bridge_server_openai.bat | 起動補助 | `OPENAI_API_KEY` と Node.js の有無を確認してから `node server.js` を起動する。 | 無 | OpenAI連携前提の起動用バッチ |

## bridgeファイルごとの補足

### `README.txt`
- 役割：bridgeパッチ同梱物の簡易説明
- 主内容：
  - 同梱ファイル一覧
  - CPU打牌external連携の概要
  - `discard` endpoint 追加
  - `cpu_snapshot.js` 同梱理由
- 注意：実行ファイルではなく説明用

### `server.js`
- 役割：ローカル橋渡しAPI本体
- 主内容：
  - `POST /cpu/open-call-decision`
  - `POST /cpu/discard-decision`
  - `GET /health`
  - `GET /debug/config`
  - `GET /debug/last-open-call`
  - `GET /debug/open-call-history`
  - `GET /debug/last-discard`
  - `GET /debug/discard-history`
- 特徴：
  - OpenAI Responses API を使用
  - `OPENAI_API_KEY`、`OPENAI_MODEL`、`OPENAI_TIMEOUT_MS` などを環境変数から読む
  - OpenAI失敗時は `auto` を返してブラウザ側内部AIへフォールバック
  - open-call と discard の履歴をサーバー側で保持
- 注意：
  - ブラウザ本体の `play*.html` が直接読むJSではない
  - `cpu_api_bridge.js` から呼ばれる外部サーバー側実装

### `start_bridge_server.bat`
- 役割：bridgeの最小起動バッチ
- 主内容：
  - バッチ配置フォルダへ移動
  - `node server.js`
  - 終了後 `pause`
- 注意：APIキー確認はしない

### `start_bridge_server_openai.bat`
- 役割：OpenAI連携前提の起動バッチ
- 主内容：
  - `OPENAI_API_KEY` 未設定チェック
  - Node.js 存在チェック
  - `node server.js`
  - 終了後 `pause`
- 注意：
  - OpenAIキー未設定なら起動前に止める
  - echo文面は `D2`、`server.js` 本体ログは `D3` 表記で少しズレがある

## 運用メモ
- render系は描画専用として扱う。
- 現在の主編集担当は **Codex / Claude Code**、この台帳は **責務整理と修正対象特定の基準** として使う。
- 台帳更新は、必ず **最新版の実ファイル確認後** に行う。
- `main.js / turn.js / actions.js / call.js / kan.js / pei.js / riichi.js / seisan.js` は局進行・状態変更の主担当。
- `turn2.js / furiten.js / doubleRonOverlayPatch.js` は既存処理へ後付けで差し込むパッチ層。
- MB体験版は `index_mbtrial.html / play_mbtrial.html / mbtrial_ui.js` を中心に独立運用し、クーポンは `coupon_admin.html / mbtrial_coupon_admin.js / mbtrial_coupon_client.js` で別管理する。
- アプリ版は `index_app.html / play_app.html / replay_app.html / replay_view_app.html / analysis_app.html / app_play_ui.js / match_log.js / log_normalizer.js / log_metrics.js / replay_app_ui.js / replay_view_app_ui.js / analysis_ui.js` を中心に土台を作る。半荘ログ保存は現行で **IndexedDB優先 / localStorage移行・フォールバック併用** として扱う。
- 原因特定や修正時は、**すでに見たファイルでも古い版の可能性を前提に、必ず最新版を再提示してもらってから判断する**。
- 画面役割の現時点方針は、**成績管理ページ=対局モードの自分成績**、**分析ページ=検証モードの全体傾向/ルール分析** として分ける。
- CPU系は「本番行動」と「評価/ログ/プロファイル」に分けて見ると追いやすい。
